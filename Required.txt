AI Data Metrics Agent - Fixes Required

1. Fix indentation around:
   - generate_chart() -> else:return None
   - get_routing_prompt() -> return f"""...
   - parse_llm_response()
   - Excel file loading loop
   - Query details expander block

2. SQL safety:
   Allow only SELECT queries before execution.

3. Replace:
   match = re.search(r"\{[^{}]*\}", response, re.DOTALL)
   with a more robust JSON parser.

4. Add chat history truncation:
   messages = messages[-20:]

5. Validate Ollama connection before use.

The uploaded notebook itself appears structurally intact; the syntax errors were introduced in the pasted Streamlit export version shared earlier.
